@using CarDealership.Models;

@if (Model.Count == 0)
{
  <p>There are no items in the list.</p>
}
<h1>Car Dealership</h1>
<ul>
  @foreach (Car car in Model)
  {
    <li>@car.Description, $@car.Price</li>
  }
  
</ul>
<a href="/cars/new">Add a new used car.</a>